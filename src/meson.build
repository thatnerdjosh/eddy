inc = include_directories('.')
executable('eddy', [
    'eddy.c'
  ],
  include_directories: inc,
  dependencies       : [ deps, eeze_dep ] ,
  gui_app            : true,
  install            : true)
